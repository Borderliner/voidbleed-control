# Template file for 'voidbleed-control'
pkgname=voidbleed-control
version=0.3.1
revision=1
build_style=go
go_import_path="github.com/Borderliner/voidbleed-control"
go_package="${go_import_path}/cmd/voidbleed-control"
# Only what it cannot work without. flatpak, fwupd, ufw and btrfs-progs are
# each optional: the matching section says how to install them when missing.
depends="xbps sudo runit"
short_desc="Voidbleed control centre: packages, services, appearance, firewall"
maintainer="Mohammadreza Hajianpour <hajianpour.mr@gmail.com>"
license="MIT"
homepage="https://github.com/Borderliner/voidbleed-control"
distfiles="${homepage}/archive/refs/tags/v${version}.tar.gz"
checksum=1c48d2d503a7408ae77027d8d918ce3dd3b12cefdedfe2e1b717c3c4949e8039

post_install() {
	vlicense LICENSE
	# From the source tree, not files/: anyone building this from the release
	# tarball has everything they need in it.
	vinstall packaging/voidbleed-control.desktop 644 usr/share/applications
}
